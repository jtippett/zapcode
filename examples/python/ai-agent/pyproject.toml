[project]
name = "zapcode-ai-agent-example"
version = "0.0.1"
description = "Zapcode AI agent examples (Python)"
requires-python = ">=3.10"
dependencies = [
    "zapcode",
    "zapcode-ai",
    "anthropic>=0.39.0",
]
